# Forbidden constructs

This paragraph has **bold with *nested emphasis* inside** it (rule 9a).

An anchor whose span carries :mark[formatting like **bold**]{#t1} inside it (rule 9b).

A bare :mark directive with no id written in running prose (rule 9c).

A raw line break here<br>and it continues on (rule 9d).
